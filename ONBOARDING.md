# 키즈덤AI (KidsDerm AI) — 온보딩 가이드

> 2026 K-Health 미개방 의료데이터 활용 경진대회 예선 제출작. 소아 피부질환(주로 아토피피부염)의
> 경과를 부모가 찍은 사진으로 정량화하고 시계열로 추적하는 **비진단 보조 도구**.
> 팀명: 키즈is프리덤. 이 문서는 새로 합류하는 팀원이 코드를 처음 열었을 때 필요한 맥락을 한 번에
> 주기 위한 것입니다 — 순서대로 읽으면 됩니다.

---

## 1. 가장 먼저 볼 문서 (`docs/` 폴더)

git에는 안 올라가 있던 기획 문서들을 이번에 `docs/`로 옮겨서 커밋했습니다. **읽는 순서**:

1. `키즈덤AI_제안서.pdf` — 메인 제안서. 문제 정의, 8단계 파이프라인, 기술적 근거, 검증 계획이 다 여기 있습니다. 코드보다 먼저 이걸 읽어야 "왜 이렇게 만들었는지"가 이해됩니다.
2. `키즈덤AI_화면설계서_v1.0_텍스트추출본.txt` — **가장 최신 화면 스펙(27개 화면)**. 원본 PDF(`키즈덤AI_화면설계서.pdf`)가 로컬에서 유실돼서 텍스트만 추출해 남겼습니다 — 원본이 필요하면 프로젝트 대표에게 다시 요청하세요. **이 문서가 지금 프론트엔드 구현의 기준입니다.**
3. `키즈덤AI_화면명세서_v1.0.xlsx` — 구버전 화면 스펙(46개 화면). 위 화면설계서로 대체됐지만, 화면 코드(CM-01-01 같은 세부 번호) 대응 관계를 찾을 때 참고용으로 남겨둠.
4. `키즈덤AI_요구사항정의서_v0.1.docx` — FR/NFR, 기능 목록표(F01~F18), 요구사항 추적 매트릭스.

---

## 2. 저장소 & 브랜치 전략

- GitHub: `https://github.com/annajeong0429-ux/KidsDerm.git`
- **4단계 브랜치**: `개인 브랜치` → `develop` (일상 통합) → `dev` (릴리즈 전 검증) → `main` (배포 가능 상태)
  - 처음엔 `main`/`develop`/개인 브랜치 3단계였는데, `develop→main` PR(#6)이 이미 머지된 뒤에 `dev`를 추가함 — 그래서 `dev`는 main과 같은 시점에서 새로 갈라져 나왔고, 이후 develop 작업은 `develop→dev` PR로 올라갑니다.
  - **새 작업은 절대 develop에 바로 커밋하지 말고 개인 브랜치를 파서 진행 후 develop으로 PR 주세요.** (지금까지는 혼자 작업이라 develop에 바로 커밋해왔지만, 팀원이 합류하면 이 규칙을 지켜야 충돌이 안 남)
  - 지금 열려있는 PR: **#7 (develop → dev)** — 최신 develop 커밋까지 반영돼 있음, 아직 미머지.

---

## 3. 프로젝트 구조

```
kidsdermAI/                    # 프론트엔드 루트 (Next.js 16, App Router, TS, Tailwind v4)
├── docs/                      # 기획 문서 4종 (위 1번 참고)
├── src/
│   ├── app/                   # 라우트 = 화면. 폴더 구조가 곧 화면설계서의 화면 목록과 대응됨
│   ├── components/
│   │   ├── layout/             # AppShell(폰 프레임+하단탭바), BottomTabBar, ScreenHeader
│   │   ├── auth/AuthGate.tsx    # 로그인 필요 화면을 감싸는 가드
│   │   └── ui/                  # Button, Card, Badge, Checkbox, IntensitySlider, Disclaimer 등
│   └── lib/
│       ├── types.ts             # 도메인 타입
│       ├── mock-data.ts         # 백엔드 없는 도메인 데이터(사례·사진·진단 등) 전부 여기 목데이터
│       ├── auth-context.tsx     # 실제 로그인 상태 관리 (백엔드 연동됨)
│       ├── child-profile-context.tsx  # 아이 프로필 - 아직 백엔드 없어서 localStorage에 저장
│       ├── record-context.tsx   # 촬영 흐름(RC) 다단계 상태 공유
│       └── medical-context.tsx  # 진단·처방 입력(MD) 다단계 상태 공유
├── backend/                   # FastAPI 인증 서버 (아래 5번 참고)
├── docker-compose.yml         # web + backend + mysql 한 번에 기동
└── scripts/screenshot.js      # Playwright로 화면 스크린샷 뜨는 개발용 스크립트
```

---

## 4. 로컬에서 실행하기

```bash
docker compose up -d
```

- 프론트엔드: http://localhost:3000
- 백엔드 API 문서: http://localhost:8001/docs (⚠️ **8000번이 아니라 8001번** — 이 컴퓨터에 다른 프로젝트가 8000을 이미 쓰고 있어서 옮김. 팀원 컴퓨터에서는 8000이 비어있으면 `docker-compose.yml`의 포트를 8000으로 되돌려도 됨)
- DB: localhost:3308 (mysql, root/pw1234)

**테스트 계정**: `consentcheck@example.com` / `Password123!` (테스트아이 프로필 등록돼 있음). 새로 회원가입해서 써도 됨.

### 알아두면 좋은 로컬 이슈
- **컨테이너를 오래 띄워두다 파일을 새로 추가하면** Turbopack의 파일 감시가 가끔 안 먹어서 새 라우트가 404가 뜹니다 → `docker compose restart web`으로 해결됨. 기존 파일 **수정**은 대체로 핫리로드가 잘 됩니다.
- 컨테이너 최초 기동 시 MySQL이 완전히 준비되기 전에 backend가 먼저 뜨려다 한 번 실패하는 경우가 있음 → `docker compose restart backend` 한 번이면 해결.

---

## 5. 지금까지 뭐가 됐고 뭐가 안 됐는지

### 프론트엔드: 최신 화면설계서(27개 화면) 기준 **전부 구현 완료**

| 대분류 | 개수 | 상태 |
|---|---|---|
| CM (공통·온보딩) | 5 | ✅ (CM-02는 3단 슬라이드 스펙을 단일 화면으로 의도적으로 단순화함) |
| HM (홈) | 2 | ✅ |
| RC (기록) | 5 | ✅ |
| AN (분석결과) | 5 | ✅ (AN-03 "중증도·면적 결과"가 최근에 새로 추가된 핵심 차별화 화면) |
| MD (진료) | 2 | ✅ (2개 화면으로 나눠 구현) |
| TR (경과관찰) | 5 | ✅ (TR-03은 면적/4징후 토글 + 처방시점 표시까지 완성) |
| EP (역학정보) | 1 | ✅ |
| ST (설정) | 3 | ✅ |

**구버전(46개) 스펙에서 넘어온 여분 화면들** (새 설계서엔 없지만 코드엔 남아있음, 기능은 정상 동작):
`/notifications`, `/notifications/reminder`, `/settings/data`, `/settings/notifications`,
`/analysis/outbreak-context`, `/analysis/next-visit`, `/cases/[caseId]/history*`, `/record/gallery`, `/record/done`.
지울지 유지할지는 아직 안 정했습니다.

### 백엔드: **인증만 실제로 동작, 나머지는 전부 미착수**

`backend/`는 팀에서 예전에 만든 **Re:medi**(`github.com/annajeong0429-ux/-Re-medi`)라는 다른 프로젝트의 로그인 모듈만 떼어와서 이식한 것입니다. 원본은 복약관리 앱이라 훨씬 큼 — 로그인/회원가입/JWT 부분만 가져오고 나머지(복약, 식단, 채팅 등)는 다 뺐습니다.

**동작하는 것**: 회원가입, 로그인, 토큰 갱신(리프레시 토큰 로테이션 + 재사용 탐지), 로그아웃, 회원탈퇴, 카카오/네이버/구글 소셜 로그인(단, 실제 CLIENT_ID/SECRET은 `.env`에 아직 안 채워져 있어서 버튼 눌러도 에러남), `/users/me`.

**보안 강화 3종 추가함**:
1. `ENV=prod`인데 `SECRET_KEY`가 없거나 32자 미만이면 서버가 아예 안 뜨게 막음
2. 회원가입 시 약관/건강정보 동의 **시각**을 서버가 직접 찍어서 DB에 기록 (클라이언트 시각 안 믿음)
3. 로그인/회원가입에 IP 기준 rate limiting (같은 IP로 로그인 10회/분, 가입 5회/분 초과 시 차단) — 계정 단위 잠금(5회 실패)과는 별개 방어선

**안 되는 것 (전부 프론트엔드 목데이터로만 존재)**:
- 병변 분할·분류·중증도 AI 모델 자체 — **이건 원래 이 저장소 범위가 아님**. 제안서 4.8절 기준으로 로컬 Jupyter Notebook에서 별도로 검증하는 트랙입니다. 웹앱 쪽은 항상 목데이터로 결과를 흉내내는 구조로 설계돼 있습니다.
- 사진/증상/진단/처방/사례 데이터 저장 (전부 `src/lib/mock-data.ts`)
- 아이 프로필 저장 (백엔드 없어서 브라우저 `localStorage`에만 있음 — `child-profile-context.tsx`)
- 질병관리청 공공데이터 API 연동 (목데이터)

**아직 해결 안 된 보안 백로그** (GitHub Issues #1~#5, `security` 라벨) — 실제 건강 데이터를 저장하기 시작하면 처리해야 함: 이메일 인증, 민감정보 필드 암호화, 보안 감사 로그, 2FA, 로그인 세션 목록/원격 로그아웃.

---

## 6. 디자인 시스템

- 배경: 따뜻한 크림색 / 주요 버튼·UI: 근블랙(near-black) / 브랜드 아이덴티티: 초록 새싹 로고+워드마크 (버튼 색으로는 안 씀, 로고에만 사용)
- 색상 토큰은 전부 `src/app/globals.css`의 `@theme inline` 블록 하나에 모여있음 (Tailwind v4 CSS-first 설정, `tailwind.config.js` 없음) — 팔레트를 통째로 바꿔야 하면 이 파일만 고치면 전체 화면에 반영됨
- 소셜 로그인 버튼은 카카오(`#fee500`)/네이버(`#03c75a`) 실제 브랜드 컬러 사용

---

## 7. 핵심 아키텍처 패턴 (반드시 알아야 헤매지 않음)

- **`AuthGate`** (`src/components/auth/AuthGate.tsx`): 로그인 안 하면 못 보는 화면을 감싸는 컴포넌트. 기록/진료/분석/사례/알림/프로필/설정 일부가 이걸로 보호됨. 로그인 안 된 채로 접근하면 `/login?redirect=원래경로`로 보냈다가 로그인 성공하면 원래 경로로 돌려보냄. 홈 화면(`/`)은 예외 — 화면 자체는 항상 보이고, 개인화된 섹션만 로그인 안 하면 "로그인하세요" 카드로 대체됨(연성 게이트).
- **다단계 입력 흐름의 상태 공유**: `/record/*`(기록), `/medical/*`(진단·처방)처럼 여러 화면에 걸친 입력은 각 폴더의 `layout.tsx`가 React Context Provider로 감싸서, 그 하위 화면들끼리 상태를 공유합니다. 새로운 다단계 흐름을 또 만들 땐 이 패턴을 그대로 따라 하면 됩니다.
- **`AppShell`**: 모든 화면을 `max-w-[430px]` 폰 프레임 안에 넣고, 지정된 4개 루트 경로(`/`, `/cases`, `/notifications`, `/settings`)에서만 하단 탭바를 보여줍니다.

---

## 8. 다음에 할 일 후보

- 백엔드에 실제 도메인 API(사진 업로드, 진단/처방 저장, 사례 조회) 붙이기 — 지금은 프론트가 전부 `mock-data.ts`를 봄
- `child-profile-context`를 localStorage 대신 실제 백엔드로 이전
- 화면설계서에서 빠진 예전 화면들(알림 등) 정리 여부 결정
- 보안 백로그 이슈 #1~#5 처리 (실 데이터 저장 시작 전 필수)
- Docker/네이티브 앱(Capacitor) 배포 방향 결정 — 아직 미정 상태로 보류 중
